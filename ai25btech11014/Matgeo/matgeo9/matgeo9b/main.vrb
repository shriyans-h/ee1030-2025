\frametitle{Python Code — With .so or Executable}
\begin{lstlisting}[language=Python]
import subprocess

# Input for C program
input_str = "4 1"

result = subprocess.run(
    ['./reflect_translate'],  # compiled C binary
    input=input_str,
    capture_output=True,
    text=True
)

print(result.stdout.strip())
\end{lstlisting}
