\frametitle{Python Code: Using .so (2/2)}
\begin{lstlisting}[language=Python]
D = np.array([2.0, 2.0, 3.0])
L = np.zeros(3)

dir_cos(D.ctypes.data_as(ctypes.POINTER(
        ctypes.c_double)),
        L.ctypes.data_as(ctypes.POINTER(
        ctypes.c_double)))

print("Direction Cosines:", L)
\end{lstlisting}
