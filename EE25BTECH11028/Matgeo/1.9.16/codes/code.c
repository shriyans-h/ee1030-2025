#include <stdio.h>

int main() {
    // Given points A and B on x-axis
    double a = -2.0;
    double b = 6.0;

    // Calculate p = (a + b) / 2
    double p = (a + b) / 2.0;

    printf("Coordinates of P are: (%.2f, 0)\n", p);

    return 0;
}
